# 🍽️ Recipe Manager

Application fullstack permettant de gérer des recettes : titre, catégorie, étapes, ingrédients et image.

---

## 🚀 Stack
- **Frontend** : React, React Router, Hooks  
- **Backend** : API PHP (REST), Upload d’images  
- **Format** : JSON + FormData

---

## ✨ Fonctionnalités
- CRUD Recettes  
- CRUD Ingrédients (dans React)  
- Upload / remplacement d’image  

---

## 📂 API
- `GET /api/recipes`
- `GET /api/recipes/:id`
- `GET /api/recipes/:id/ingredients`
- `POST /api/recipes`
- `PATCH /api/recipes/:id`
- `POST /api/recipes/:id/image`

---

## ⚙️ Installation
```bash
npm install
npm run dev
